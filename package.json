{
    "name": "TP1",
    "version": "1.0.0",
    "type": "module",
    "devDependencies": {
        "jest": "^29.3.1",
        "mocha": "^10.2.0"
    },
    "scripts": {
        "test": "mocha",
        "test:cov": "c8 -r=html -r=text mocha",
        "test:cov:watch": "c8 -r=html -r=text mocha --watch --parallel"
    },
    "dependencies": {
        "babel-jest": "^29.3.1",
        "c8": "^7.12.0",
        "ts-jest": "^29.0.3"
    }
}
